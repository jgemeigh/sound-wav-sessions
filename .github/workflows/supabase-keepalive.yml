name: Supabase Keepalive

on:
  schedule:
    - cron: "17 */12 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping project health endpoint
        run: |
          curl --fail --show-error --silent \
            --request GET \
            "https://dafqbhphoeblxpfrizwx.supabase.co/functions/v1/project-health"
